pre-commit:
  parallel: true
  commands:
    check:
      run: pnpm run check --staged --fix --no-errors-on-unmatched
      stage_fixed: true
    test:
      run: pnpm run test

post-commit:
  commands:
    agent-watch:
      run: npx agent-watch run
